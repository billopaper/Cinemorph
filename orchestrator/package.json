{
  "name": "cinemorph-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cinemorph orchestrator: holds the story graph, runs the diff-and-render pipeline, serves the player.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "render": "node render.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
